version: 2.1
jobs:
  deployDesarrollo:
    docker:
      - image: circleci/openjdk
    steps:
      - add_ssh_keys:
          fingerprints:
            - "70:ac:95:44:2d:b1:d5:0e:8b:11:f0:d6:f2:0b:6c:1d"
      - checkout
      - run:
          name: "Deploy develop"
          command: |
            chmod 600 secondtry.pem
            scp -v -o StrictHostKeyChecking=no -i secondtry.pem -r index.html ec2-3-84-99-215.compute-1.amazonaws.com:/~/var/www/html/index.html
workflows: 
  version: 2
  Ejecutar:
    jobs:
      - deployDesarrollo